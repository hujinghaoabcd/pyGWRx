# Dataset licensing

Bundled datasets retain their upstream licenses. See the repository-level
`DATA_LICENSES.md`, `THIRD_PARTY_NOTICES.md`, and `DATA_HASHES.sha256` for the
complete redistribution record. Package metadata returned by `get_dataset_info()`
and `load_dataset(..., return_type="dict")` includes license, source URL, and
processing information.
